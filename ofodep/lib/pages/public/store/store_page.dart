import 'package:flutter/material.dart';

class StorePage extends StatelessWidget {
  final String? storeId;
  const StorePage({super.key, this.storeId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
